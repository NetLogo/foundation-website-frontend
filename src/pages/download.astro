---
import Layout from "../layouts/Layout.astro";
import {DownloadSection} from "../components/download/download-page.tsx";
import NetLogoApi from "../utils/api.js";

const api = new NetLogoApi();

const versions = await api.getNetLogoVersions()

---

<Layout>
    <DownloadSection versions = {versions["netlogo_versions"]} client:load/>
</Layout>